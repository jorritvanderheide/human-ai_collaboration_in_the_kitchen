// setup
function setup() {
  createCanvas(windowWidth, windowHeight);
  noLoop();
}

// draw
function draw() {

  // // setup neural network
  // const options = {
  //   input: 50,
  //   outputs: 1,
  //   task: 'regression'
  // }
  // nn = ml5.neuralNetwork(options);

  // // load training data
  // trainingData = loadTable();

  // // train model
  // nn.train();

  // // save model
  // nn.save();
}